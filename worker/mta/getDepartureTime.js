// TODO: get the next departure time after now that the routeId train
// will leave a given station
// routeId: "A", "C", or "E"
// stopId is the station (e.g. "A12N")
// You can solve this leetcode problem yourself you delegate to
// an AI to do it. It doesn't matter how this gets done as long as
// all the unit tests are passed. The unit tests are created from
// OpenAPI spec (TBD)
export const getDepartureTime = (data) => ({ stopId, routeId, now }) => {
  return ""
}